"""
Package marker for Django migrations in the accounts app.

This file ensures that the ``migrations`` directory is treated as a
Python package.  Actual migration files are generated via
``python manage.py makemigrations``.
"""
