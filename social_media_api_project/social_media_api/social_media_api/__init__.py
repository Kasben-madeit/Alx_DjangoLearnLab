"""
Package initializer for the ``social_media_api`` Django project.

This file is intentionally left blank but exists so that Python
treats the ``social_media_api`` directory as a package.  All project
configuration lives in ``settings.py`` and URL routing in
``urls.py``.
"""
