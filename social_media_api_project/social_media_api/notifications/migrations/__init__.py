"""
Package marker for Django migrations in the notifications app.

This file ensures that the ``migrations`` directory is recognised as
a package.  Create migration files via ``python manage.py makemigrations``.
"""
